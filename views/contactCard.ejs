<!DOCTYPE html>
<html>
<head>
    <% include head %>
</head>
  <body>
  <% include header %>
  <div class="container-fluid">
    <section class="container">
      <div class="container-page">
        <a class="btn btn-danger" href="/" style="margin-top: 10px;"> <= Home</a>
        <div class="card">
          <img class="card-img-top" src="<%= employeeList.picture %>" alt="<%= employeeList.name %>" style="width:100%"/>
          <div class="container">
            <h4><b><%= employeeList.name %></b></h4>
            <p><%= employeeList.gender %>  <%= employeeList.age %></p>
            <p><%= employeeList.email %></p>
          </div>
        </div>
      </div>
    </section>
  </div>
  <% include footer %>
  </body>
</html>

